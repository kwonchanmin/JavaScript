
function myFunc() {
    // 삭제
    // $('#innerDiv').remove();
    // 숨김
    // $('#innerDiv').hide();
    // 숨겨진거 다시 볼려면 show()다.

    // 삭제(후손만 삭제)
    // $('#innerDiv').empty();
}


function display_input() {
    alert($('#myID').val());
}